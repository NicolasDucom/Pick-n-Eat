{% include 'header.html.twig' %}
	<div class="container">
		<div class="row">
			<div id="intro_show" class="col s10 offset-s1">
				<p id="intro_text">{{ app.translator.trans('main_description') }}</p>
			</div>
		</div>
		<div class="row">
			<div class="col s3 offset-s3">
				<p><a href="{{ app.url_generator.generate('choose') }}" class="waves-effect waves-light btn-large btn-black"><i class="fa fa-random"></i>{{ app.translator.trans('roll') }}</a></p>
			</div>
			<div class="col s4">
				<p>{{ app.translator.trans('roll_description') }}</p>
			</div>
		</div>
		<div class="row">
			<div class="col s3 offset-s3">
				<p><a href="{{ app.url_generator.generate('random') }}" class="waves-effect waves-light btn-large btn-green"><i class="fa fa-pencil"></i>{{ app.translator.trans('picky') }}</a></p>
			</div>
			<div class="col s4">
				<p>{{ app.translator.trans('picky_description') }}</p>
			</div>
		</div>
	</div>

	<script type="text/javascript" src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
	<script type="text/javascript" src="{{ app.request.basepath }}/js/materialize.min.js"></script>
	<script type="text/javascript" src="{{ app.request.basepath }}/js/index.js"></script>
</body>
</html>